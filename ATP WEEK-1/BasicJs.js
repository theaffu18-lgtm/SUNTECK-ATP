// Task: Calculate total amount after adding items and applying tax

let totalAmount = 0;
//adding amount to total amount of 500
totalAmount += 500;
// adding 1200
totalAmount += 1200;
// remove of 200 of amount
totalAmount -= 200;
// GST of totla amount of 18%
totalAmount = (totalAmount * 18) / 100 + totalAmount
console.log("Total amount after tax is ", totalAmount)